# 04 — IaC & Deployments

ตัวอย่าง config **Serverless Framework**, **Terraform**, และ traffic shifting (canary)

```bash
node 03-expert/examples/04-iac-deployments/validate-config.js
```

ไฟล์ใน folder นี้เป็น **template เพื่อเรียนรู้** — ยังไม่ deploy จนกว่าจะใส่ credential / ปรับ account
